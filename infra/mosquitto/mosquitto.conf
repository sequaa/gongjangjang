# Skeleton broker config (D-03). Anonymous, unencrypted, docker-network only.
# Auth/TLS are intentionally deferred (Phase 4) — see 01-RESEARCH Security Domain.
listener 1883
allow_anonymous true
persistence false
log_dest stdout
